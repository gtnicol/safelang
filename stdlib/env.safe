module env;

public string get(string name) {
    return getenv(name);
}

public boolean has(string name) {
    return getenv(name) != "";
}

public string require(string name, string fallback) {
    const string value = getenv(name);
    return if (value != "") then value else fallback;
}
